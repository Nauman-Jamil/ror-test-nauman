<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
  <div class="text-center py-12">
    <div class="mx-auto h-12 w-12 text-green-400">
      <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
    </div>
    <h3 class="mt-2 text-sm font-medium text-gray-900">Impact metric created successfully</h3>
    <p class="mt-1 text-sm text-gray-500">The impact metric has been created and is now available for analysis.</p>
    <div class="mt-6">
      <% if @budget.present? %>
        <%= link_to "View Budget", budget_path(@budget), class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        <%= link_to "Create Another", new_budget_budget_allocation_impact_metric_path(@budget, @budget_allocation), class: "ml-3 inline-flex items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      <% else %>
        <%= link_to "View Metric", impact_metric_path(@impact_metric), class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        <%= link_to "Back to Metrics", impact_metrics_path, class: "ml-3 inline-flex items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      <% end %>
    </div>
  </div>
</div>
